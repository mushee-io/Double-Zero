# Mushee — Network Observatory (Demo)

No `@/` imports. Everything is relative and lives inside `/app` so Vercel can't get confused.

## Run
npm install
npm run dev

## WalletConnect
Set `.env.local`:
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=...
